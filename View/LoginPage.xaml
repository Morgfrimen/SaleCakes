<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage x:Class="SaleCake.View.LoginPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SaleCake.View">
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackLayout VerticalOptions="CenterAndExpand"
                Grid.Column="1">
                <Entry Text="Логин"/>
                <Entry IsPassword="True"
                       Text="Пароль"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>